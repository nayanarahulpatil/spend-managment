Use the existing testing stack.

Before generating tests:
- Validate package.json dependencies.
- Validate tsconfig.json.
- Validate Jest/Playwright configuration.
- Identify missing dependencies, types, setup files, and scripts.

Generate:
- package.json updates
- installation commands
- configuration files
- setup files
- executable test code

Ensure all generated tests compile and run without TypeScript errors.